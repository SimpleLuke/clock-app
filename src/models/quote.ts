class NewQuote {
  id: string;
  author: string;
  en: string;

  constructor(id: string, author: string, en: string) {
    this.id = id;
    this.author = author;
    this.en = en;
  }
}

export default NewQuote;
